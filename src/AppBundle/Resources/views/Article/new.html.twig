{% extends "AppBundle:Layout:layout.html.twig" %}
{% form_theme form 'bootstrap_3_layout.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block content %}
    
    <div class="row">
        <div class="col-sm-8 col-sm-offset-2">
            <div id="legend">
                <legend class="">Dodaj artykuł</legend>
            </div>
        </div>
    </div>
    
    {{ form_start(form) }}
    {{ form_widget(form) }}


    <div class="row">
        <div class="col-xs-8 col-sm-10 col-md-10">
            <div class="form-group">

            </div>
        </div>
        <div class="col-xs-4 col-sm-2 col-md-2">
            <div class="form-group">
                <button type="submit" id="account_submit" class="form-control btn btn-primary">Dodaj artykuł</button>
            </div>
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}